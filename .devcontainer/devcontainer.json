{
  "name": "Anaconda + Postgres",
  "image": "mcr.microsoft.com/devcontainers/anaconda:latest",
  "forwardPorts": [5432, 8765],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y postgresql postgresql-contrib && sudo service postgresql start && conda update -n base -c defaults conda -y"
}